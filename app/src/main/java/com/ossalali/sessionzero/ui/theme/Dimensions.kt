package com.ossalali.sessionzero.ui.theme

import androidx.compose.ui.unit.dp

/**
 * Standard dimension constants used throughout the Session Zero app.
 * Use these instead of hardcoded dp values to ensure visual consistency.
 */
object Dimens {

    // ==================== Spacing ====================

    /** 2.dp — Border stroke width */
    val BorderStroke = 2.dp

    /** 4.dp — Tiny spacing between tightly grouped elements */
    val SpacingTiny = 4.dp

    /** 8.dp — Small spacing (list items, inner gaps, chip spacing) */
    val SpacingSmall = 8.dp

    /** 12.dp — Medium spacing (card gaps, grid arrangement) */
    val SpacingMedium = 12.dp

    /** 16.dp — Standard spacing (section gaps, screen padding) */
    val SpacingLarge = 16.dp

    // ==================== Padding ====================

    /** 8.dp — Small padding (inner card content, step indicator) */
    val PaddingSmall = 8.dp

    /** 12.dp — Medium padding (DndCard inner content) */
    val PaddingMedium = 12.dp

    /** 16.dp — Standard screen/container padding */
    val PaddingLarge = 16.dp

    // ==================== Component Sizes ====================

    /** 32.dp — Small input width (e.g., score display) */
    val InputWidthSmall = 32.dp

    /** 48.dp — Standard button/stat element height */
    val ButtonHeight = 48.dp

    /** 56.dp — Medium stat box width */
    val StatBoxSmall = 56.dp

    /** 64.dp — Ability score box width */
    val AbilityScoreBoxWidth = 64.dp

    /** 72.dp — Standard stat shape size (Circle, Hexagon, Rectangle, Shield width) */
    val StatShapeStandard = 72.dp

    /** 80.dp — Large stat shape size (Diamond, Heart) and empty state icon */
    val StatShapeLarge = 80.dp

    /** 82.dp — Shield shape height */
    val ShieldShapeHeight = 82.dp

    // ==================== Input / Label Widths ====================

    /** 80.dp — Narrow input field width */
    val InputWidthNarrow = 80.dp

    /** 100.dp — Standard label width */
    val LabelWidth = 100.dp

    /** 120.dp — Dropdown/selector width */
    val DropdownWidth = 120.dp

    /** 200.dp — Wide stat/input element width */
    val InputWidthWide = 200.dp

    // ==================== Corner Radius ====================

    /** 16.dp — Standard rounded corner shape radius */
    val CornerRadiusStandard = 16.dp

    // ==================== Icon Sizes ====================

    /** 80.dp — Large icon (e.g., empty state illustration) */
    val IconSizeLarge = 80.dp
}
